# Session Handoff — CHỐT PHIÊN 23/06/2026 (Claude — S35: phân hệ chấm AI tự luận + đề thi tiếng Anh hoàn chỉnh, tất cả LIVE)

> **TRẠNG THÁI CHỐT PHIÊN (đọc đầu tiên):** `origin/main` = `origin/Dat` = **`4d8cb96`** (C13→C18 + **C19 đề B1 đầy đủ 4 kỹ năng + C20 vá chấm Gemini THẬT** đều đã DEPLOY LIVE 24/06; + commit chốt-sổ docs này trên Dat). Working tree SẠCH. Clean-state toàn xanh: pytest **60 passed / 0 failed**, traceability 4/4, ruff sạch, architecture PASS, `npm run build` + `tsc` PASS. Spec **42 — 41 active / 0 gap / 1 planned**. **Verify LIVE C19+C20**: `GET /exams` → B1 đầy đủ 14 câu; nộp end-to-end completed (mc 3 / writing 16 MOCK / speaking 0 do guard không-audio / total 19, không crash); Render vẫn mock (chưa đặt `GEMINI_API_KEY`).
> **❗ PHÁT HIỆN QUAN TRỌNG (C20):** backend/.env **LOCAL có key Gemini thật**; real-mode từng HỎNG (`gemini-1.5-flash` bị Google gỡ→404; Nói-không-audio crash). Đã vá: model qua env `GEMINI_MODEL`=`gemini-2.5-flash` + guard audio rỗng. **Verify bằng Gemini thật: Viết chấm 6.5 + feedback thật, Nói-no-audio 0.0 không crash.** Render vẫn mock (chưa có key) — đặt `GEMINI_API_KEY` ở Render để bật real LIVE. **CHƯA đụng tiếng Trung dòng nào** (chỉ cột/comment nền có sẵn).
> 🏁 **PHÂN HỆ TIẾNG ANH HOÀN CHỈNH & LIVE**: TOEIC (sinh đề + chấm trắc nghiệm) · VSTEP B1 (Đọc trắc nghiệm + **Viết** + **Nói**) · **chấm AI tự luận** (Gemini, ghi âm/upload audio) · review loop (thí sinh + giáo viên đọc/nghe bài) · **human-in-the-loop** (giáo viên sửa điểm AI) · **đồng hồ đếm ngược + tự nộp**. Phiên S35 làm: GRADE-003 · C6 · C7 · C8 · C9 · C10 · C11 (SPEC-SUBMIT-003) · C12 — Claude tự code + tự nghiệm thu + deploy từng phần (Đạt cho phép minh bạch mỗi lần).
> ⚙️ **Chấm AI LIVE = MOCK** (Render free không Celery worker → chạy nội tuyến qua `CELERY_TASK_ALWAYS_EAGER`/fallback `.apply`; chưa đặt `GEMINI_API_KEY`). Đặt `GEMINI_API_KEY` ở env Render = bật chấm Gemini THẬT, KHÔNG cần sửa code.
> **✅ C13 (S36, 23-24/06) — PROCTORING phòng thi (🚀 ĐÃ DEPLOY LIVE):** wire `useProctoring` vào `TakeView` — consent gate (giải thích webcam+toàn màn hình+vi phạm) → vào phòng bật webcam+fullscreen → webcam PiP + badge "Đang giám sát" + counter `⚠ N/3` + banner cảnh báo + **tự nộp khi vi phạm 3 lần** + đồng hồ khởi động lúc vào phòng. Frontend thuần (chỉ `TakeView.tsx`+`feature_list`). Verify browser thật (demo DB): consent→start→counter→3 vi phạm→tự nộp "10đ COMPLETED"; `npm run build` PASS. `feature_list.exam-room-ui` → **active** (không còn feature `not_started`). **🚀 DEPLOY LIVE (24/06, Đạt xác nhận dialog):** FF-push `Dat→main` → `origin/main`=`origin/Dat`=**`34c2a50`**; verify LIVE: Vercel routes 200 + JS bundle production chứa chuỗi proctoring (build mới thật, không phải cũ); backend /health 200. ⚠️ Vi phạm CHỈ cưỡng chế client-side, CHƯA persist backend (báo cáo cho GV cần cột `Submission.proctoring_log` — **cần Đạt duyệt**, model đóng băng); webcam/fullscreen thật cần kiểm tay trên trình duyệt thật (headless không camera). Chi tiết: `claude-progress.md` S36.
> **✅ C14 (S36 tiếp, 24/06) — vá real-mode chấm AI phần NÓI (🚀 ĐÃ DEPLOY LIVE `70e68e9`; verify LIVE: nộp VSTEP B1 → writing 8/speaking 7.5/total 17.5 mock, không regression):** `grade_speaking` trước đây `httpx.get(audio_url)` với path tương đối `/static/uploads/...` → `UnsupportedProtocol` → real-mode Nói LUÔN 0 điểm khi cắm `GEMINI_API_KEY`. Thêm `_load_audio_bytes`: path local → **đọc đĩa** `backend/static/...` (robust Render single-host); URL http(s) → fetch httpx; mime suy từ đuôi file. Backend thuần (`ai_grading.py`+test). Verify: pytest **54/0/0** (+guard non-tautological FakeModel đọc đúng bytes đĩa), ruff/architecture sạch. Khi Đạt cắm key → Viết+Nói chấm Gemini thật. SDK `google.generativeai` EOL → migrate `google.genai` là follow-up riêng. Chi tiết: `claude-progress.md` S36/C14.
> **✅ C15 (S36 tiếp, 24/06) — phiên thi BỀN server-side (🚀 ĐÃ DEPLOY LIVE `61acff6`; verify LIVE: START 7200→AUTOSAVE→RESUME cùng attempt 7195+đáp án khôi phục→SUBMIT reuse=True→/me count=1):** dựng 2 endpoint mà AC SPEC-SCALE-003 nhắc — `POST /exams/{id}/start` (tìm-hoặc-tạo attempt in_progress, trả `remaining_seconds` tính ở SERVER → đồng hồ sống sót reload, không reset client + trả đáp án đã lưu để resume) + `POST /submissions/{id}/autosave` (upsert đáp án, owner-gated, 404 nếu đã nộp/không-phải-chủ). `create_submission_and_grade` TÁI DÙNG attempt khi nộp (backward-compat: submit không /start vẫn tạo mới) → 1 bản ghi; listings lọc bỏ attempt chưa nộp. Frontend `TakeView`: đồng hồ từ server + khôi phục đáp án + autosave 15s. **KHÔNG sửa model/spec.** Verify: pytest **56/0/0** (+2 test non-tautological: session happy-path + autosave gating); browser thật (start ⏱119:42 từ server; chọn đáp án→backend nhận autosave 200; **reload→⏱117:35 tiếp tục + đáp án khôi phục**). Đóng 2 lỗ: gian lận reload-reset-timer + mất bài khi mất kết nối. SCALE-003 load-test vẫn planned (cần staging). Chi tiết: `claude-progress.md` S36/C15.
> **✅ C16 (S36 tiếp, 24/06) — resume bài dở từ danh mục đề (🚀 ĐÃ DEPLOY LIVE `d575de1`; verify: /active BEFORE [] → START [1 attempt] → SUBMIT []; bundle có "Đang làm dở"):** `GET /submissions/active` (attempt in_progress của user, chỉ đề active, kèm exam_title + remaining server) + `/exams` hiện badge "Đang làm dở" + nút amber "Tiếp tục bài đang làm →". Hoàn thiện UX C15 (resume trước đây vô hình nếu rời trang). Backend+frontend, KHÔNG model/spec. Verify: pytest **57/0/0** (+test_active_attempts_listing) + browser (exam dở→badge+nút amber; exam thường→"Làm bài"). Chi tiết: `claude-progress.md` S36/C16.
> **✅ C17 (S36 tiếp, 24/06) — giáo viên giám sát thi LIVE (🚀 ĐÃ DEPLOY LIVE `8142392`; verify LIVE: /active-attempts candidate 403 + no-token 401 + Vercel bundle có "Đang làm bài"; teacher-positive verify đầy đủ local):** `GET /exams/{id}/active-attempts` (admin/teacher) → thí sinh đang làm bài (in_progress) kèm tên + remaining server; `ResultsView` (/admin/results/[id]) hiện panel "🟢 Đang làm bài (N)" + nút "Làm mới". Bổ sung góc nhìn giám sát phía GV cho mạch phòng-thi (C13/C15/C16). KHÔNG model/spec. Verify: pytest **58/0/0** (+test_exam_active_attempts_invigilation: start→GV thấy 1→candidate 403→submit→rỗng) + browser (panel liệt kê 3 thí sinh + "118:51 còn lại"; console sạch). Chi tiết: `claude-progress.md` S36/C17.
> **✅ C18 (S36 tiếp, 24/06) — xuất kết quả ra CSV (🚀 ĐÃ DEPLOY LIVE `7edffc0`; verify: /results.csv candidate 403 + no-token 401 + Vercel bundle có "Xuất CSV"; teacher-positive verify local):** `GET /exams/{id}/results.csv` (admin/teacher) → text/csv + BOM UTF-8 (Excel đọc tiếng Việt), tiêu đề + 1 dòng/bài nộp; `ResultsView` nút "⬇ Xuất CSV" (fetch blob → tải). Tái dùng `list_exam_submissions`, KHÔNG model/spec/service. Verify: pytest **59/0/0** (+test_export_exam_results_csv: BOM ord==0xFEFF, header, row, candidate 403, 404) + browser (nút hiện; endpoint 200 text/csv + header + 9 dòng). Chi tiết: `claude-progress.md` S36/C18.
> **🎯 VIỆC ĐANG CHỜ (phiên sau, EN-only theo Đạt — CN/HSK hoãn):** (1) ~~proctoring~~ ✅ XONG (C13, chưa deploy); (2) **(CẦN ĐẠT DUYỆT)** thêm cột `exam_type`/`language` vào model `Question` (đóng băng) → generate VSTEP/HSK từ ngân hàng thay vì seed — xem memory [[bank-thieu-cot-exam-type]]; (3) **(CẦN ĐẠT DUYỆT)** persist vi phạm proctoring về backend (cột `Submission`) cho báo cáo GV; (4) đặt `GEMINI_API_KEY` ở Render; (5) (tuỳ) deploy C13 lên main; (6) (tuỳ) timer bền lưu started_at; kiểm tay webcam/fullscreen trên trình duyệt thật. **Chi tiết S35 (GRADE-003→C12) + S36 (C13) trong `claude-progress.md`; các khối ">" bên dưới là lịch sử deploy từng phần.**
>
> ---
> **🚀 ĐÃ DEPLOY LIVE (23/06, Đạt cho phép):** `origin/main` = `origin/Dat` = **`3905396`**. Mạch chấm AI tự luận tiếng Anh (GRADE-003 async + C6 UI Viết + C7 đề VSTEP/hỗn hợp + C8 KQ mọi vai trò) nay LIVE. Render free không có Redis/worker → chấm tự luận chạy **fallback nội tuyến** (submission_admin bắt lỗi `.delay()` → `.apply()`); KHÔNG cần env eager. Verify LIVE: nộp đề VSTEP B1 (id=3, dùng ĐÚNG question_id từ `GET /exams/3`) → writing 8.0(mock)+4 trắc nghiệm = total 12 completed; frontend Vercel mọi route 200. 📌 **Chấm Gemini THẬT**: đặt `GEMINI_API_KEY` ở env Render (hiện mock 8.0). cloud_bootstrap đã seed đề Writing/VSTEP lúc build.
>
> **C9 (S35 tiếp, 23/06) — trang chi tiết bài nộp (đọc bài luận + nhận xét AI), CHƯA deploy:** `/submissions/[id]` (page + SubmissionDetailView) tái dùng `GET /submissions/{id}` (gate owner|teacher) → hiển thị đề + toàn văn bài làm + nhận xét AI từng câu + tóm tắt điểm; link "Xem" từ bảng giáo viên (ResultsView) + "Xem chi tiết" từ /my-results. Frontend thuần (không đụng backend). Verify: npm build PASS, browser (candidate đọc chi tiết; admin không-phải-chủ-bài mở được để kiểm duyệt; console 0 lỗi). 🏁 Review loop tự luận EN trọn vẹn. **🚀 ĐÃ DEPLOY C9 LÊN MAIN LIVE (23/06):** `origin/main`=`origin/Dat`=`069e483`; Vercel `/submissions/1` 404→200; backend LIVE GET /submissions trả đủ essay+feedback. **Toàn mạch chấm AI tự luận EN (GRADE-003+C6+C7+C8+C9) LIVE đồng bộ.**
>
> **C10 (S35 tiếp, 23/06) — phần NÓI (Speaking), CHƯA deploy:** `POST /api/v1/submissions/upload-audio` lưu ghi âm vào `static/uploads` (gitignored)→`audio_url`; `AnswerItem.audio_url`; submission_admin lưu lên SubmissionDetail; worker `grade_speaking` chấm khi đề có câu speaking. Frontend `TakeView` +`SpeakingItem` (ghi âm qua `useMediaRecorder` + tải file → uploadAudio), panel KQ + `/submissions/[id]` hiện phần Nói (audio player + transcription + điểm + lỗi phát âm). seed_vstep_demo +1 câu speaking. Verify: pytest **52/0/0** (+chấm Nói async + upload endpoint), npm build PASS, curl (upload→submit→mc2+viết8+nói7.5=17.5) + browser (take có nút Ghi âm/Tải file; detail hiện "Phần nói 7.5/10"+audio+transcription; console sạch). Mock vì không Gemini key. 🏁 Cặp kỹ năng tự luận AI EN hoàn tất (Viết+Nói). **🚀 ĐÃ DEPLOY C10 LÊN MAIN LIVE (23/06):** `origin/main`=`origin/Dat`=`682dd18`; Render backend (VSTEP B1 nay 6 câu Đọc+Viết+Nói; upload-audio + chấm Nói LIVE → mc2+viết8+nói7.5=17.5); Vercel routes 200. **Toàn mạch Khảo thí + chấm AI tự luận EN LIVE.**
>
> **C11 (S35 tiếp, 23/06) — giáo viên duyệt/sửa điểm AI (human-in-the-loop), SPEC-SUBMIT-003, CHƯA deploy:** `PATCH /api/v1/submissions/{id}/grade` (admin/teacher) → `override_grade` cập nhật score_writing/speaking + tính lại total + lưu teacher_note vào feedback_writing JSON; 403 candidate / 401 no-token / 404 không tồn tại. Frontend `SubmissionDetailView`: panel sửa điểm chỉ hiện cho admin/teacher (getMe); nhận xét GV hiện cho mọi vai trò. KHÔNG sửa schema model. Verify: pytest **53/0/0** (+SUBMIT-003), npm build PASS, browser (admin sửa Viết 8→6.5+note → DB lưu, tổng tính lại; thí sinh thấy điểm+note nhưng không có panel). Spec **42 — 41 active / 0 gap / 1 planned**. 🏁 Khép kín "AI gợi ý — giáo viên quyết định". **🚀 ĐÃ DEPLOY C11 LÊN MAIN LIVE (23/06):** `origin/main`=`origin/Dat`=`a68d91c`; PATCH grade route gate đúng LIVE (401 no-token / 403 candidate); Vercel routes 200. **Bước kế (EN-only):** (cần Đạt duyệt) cột exam_type→generate VSTEP từ bank · dọn make_fixtures.py · đặt GEMINI_API_KEY ở Render cho chấm Gemini thật. *(Phần tiếng Anh giờ gần như trọn vẹn: TOEIC + VSTEP Đọc/Viết/Nói + chấm AI + review loop + human-in-the-loop, tất cả LIVE.)*
>
> **C12 (S35 tiếp, 23/06) — đồng hồ đếm ngược + tự nộp khi hết giờ, CHƯA deploy:** `TakeView` thêm `remainingSec` (= duration_minutes*60), interval đếm lùi (1 interval, cleanup), tự `handleSubmit(true)` khi về 0; `handleSubmit(auto)` bỏ confirm khi auto + guard chống nộp đôi; header `⏱ MM:SS` đổi màu ≤5'/≤1'. Frontend thuần (chỉ TakeView, không backend/spec). Verify: npm build PASS, browser (đề set duration=1' → timer đếm 60→0 → **tự nộp** → kết quả "15.5đ COMPLETED, bài trống"; console sạch). Lưu ý: đếm client-side từ lúc tải (reload reset giờ) — bản bền (lưu started_at) là nâng cấp sau. **🚀 ĐÃ DEPLOY C12 LÊN MAIN LIVE (23/06):** `origin/main`=`origin/Dat`=`e47d543`; Vercel routes 200 (đồng hồ + tự nộp LIVE). **Bước kế (EN-only):** proctoring (giám thị tab/webcam) · (cần Đạt duyệt) cột exam_type→generate VSTEP · dọn make_fixtures.py · GEMINI_API_KEY ở Render cho chấm Gemini thật.

> 🎉 **DEMO LIVE + AUTH**: Frontend https://he-thong-khao-thi-va-cham-thi-tu-do.vercel.app · Backend https://toeic-backend-5n57.onrender.com. Demo giờ "đăng nhập rồi dùng": `/admin` + `/admin/bank` bắt login (admin/teacher); xem đề thí sinh vẫn mở. Admin seed từ env Render `ADMIN_USERNAME`/`ADMIN_PASSWORD` (Đạt đặt). Verify live OK (login admin → /admin, stats 7/7 đủ). Redeploy = push `main`; nếu login fail sau redeploy → Render "Clear build cache & deploy". Chi tiết auth: memory `deploy-cloud-urls`.
>
> **TRẠNG THÁI MỚI NHẤT (S35, 23/06 — thay cho phần dưới):** Suite **48 passed / 0 skipped / 0 xfailed**; spec **41 — 40 active / 0 gap / 1 planned** (chỉ còn SCALE-003 load-test, đã hoãn). 🏁 **GRADE-003 nối hàng đợi chấm AI tự luận** — `submission_admin` định tuyến theo loại câu hỏi: đề có Writing/Speaking → đẩy `grade_submission_task.delay()` + trả về NGAY `status="grading"`, worker Celery chấm ngầm qua `ai_grading_service` (Gemini, mock-safe) → ghi `Grade.score_writing/speaking` + `status="completed"`; TOEIC trắc nghiệm thuần vẫn chấm đồng bộ (SUBMIT-001 nguyên vẹn); non-TOEIC không tự luận vẫn 400. **KHÔNG sửa model** (cột tự luận đã có) → không migration. `celery-worker` feature → active. Test dùng celery eager + AI mock + monkeypatch worker SessionLocal (không cần Redis/mạng). Claude verify độc lập: pytest 48/0/0, ruff/arch/traceability sạch. ⚠️ Async thật cần Redis + `celery worker`. Trước đó S34: GRADE-002 (cột điểm L/R đúng ngữ nghĩa) + deploy C4/C5/GRADE-002 LIVE. **Chi tiết S25-S35 ở `claude-progress.md`.**
> **C6 (S35 tiếp, 23/06) — UI làm bài Tự luận + kết quả chấm AI (frontend + glue, KHÔNG spec mới):** `CELERY_TASK_ALWAYS_EAGER` (config→celery.conf) cho chấm chạy nội tuyến khi không có Celery worker/Redis (cần local demo + Render free; default False để async thật ở prod khi có worker); `scripts/seed_writing_demo.py` seed đề "VSTEP Writing Demo (AI-graded)" (2 prompt writing, idempotent); `api.ts` +`getSubmission`/`SubmissionDetail`, điểm nullable; `TakeView.tsx` textarea cho câu writing + **poll** `getSubmission` tới `completed` rồi hiện panel điểm AI + nhận xét/câu (TOEIC L/R giữ panel cũ). Verify: pytest 48/0/0, npm build PASS, curl end-to-end (submit→grading→poll→score_writing=16), **browser thật** (login tsC6→/exams→/exam/1/take 2 textarea→nộp→"16 ĐIỂM AI · COMPLETED" + nhận xét; console 0 lỗi, screenshot OK). Mock feedback vì không Gemini key (degrade-safe). **Chưa deploy.** Bật chấm Gemini THẬT: đặt `GEMINI_API_KEY` + `CELERY_TASK_ALWAYS_EAGER=true` (hoặc chạy `celery -A app.core.celery worker` cho async thật).
>
> **C7 (S35 tiếp, 23/06) — đề VSTEP B1 demo (loại đề thứ 2) + UI kết quả đề HỖN HỢP:** `scripts/seed_vstep_demo.py` seed đề "VSTEP B1 Demo (Reading+Writing, AI-graded)" — 4 câu Reading trắc nghiệm (auto) + 1 Writing (AI), gắn thẳng `exam_id` (KHÔNG vào bank dùng chung → không đụng generate TOEIC). `TakeView.tsx` panel kết quả hiện **cả** điểm trắc nghiệm ("X/Y câu đúng" từ `score_multiple_choice`) lẫn nhận xét AI phần Viết. +test guard `test_async_grading_mixed_choice_and_writing`. Verify: pytest **49/0/0**, npm build PASS, **browser thật** (VSTEP take: 16 radio+1 textarea → nộp → "8 ĐIỂM AI · COMPLETED" + "TRẮC NGHIỆM 3/4 câu đúng" + nhận xét AI; console 0 lỗi). **Chưa deploy.**
> 📌 **CẦN ĐẠT QUYẾT để đa ngôn ngữ THẬT (generate VSTEP/HSK từ bank)**: `generate_exam` đã data-driven nhưng model `Question` (đóng băng) THIẾU cột `exam_type`/`language` → bank nhiều loại đề không cùng tồn tại được. Thêm cột = ngoại lệ sửa model đóng băng, cần Đạt duyệt (như GRADE-002). Hiện VSTEP chỉ seed trực tiếp, chưa generate từ bank.
> **C8 (S35 tiếp, 23/06) — KQ bài Tự luận cho giáo viên + lịch sử thí sinh:** listing API (`list_exam_submissions`/`list_my_submissions`) + schema + `api.ts` trả thêm `exam_type`+`writing_score`; `ResultsView` (giáo viên) đổi cột "Điểm Viết (AI)" cho đề không-TOEIC; `/my-results` badge `exam_type` + thẻ hiện "Viết (AI)". Verify: pytest **49/0/0**, npm build PASS, **browser** (candidate /my-results "VSTEP·Viết(AI) 8·Tổng 12"; admin /admin/results/1 cột "Điểm Viết (AI)" 8 điểm; console 0 lỗi). **Đạt yêu cầu: hoàn thiện EN trước, CHƯA động CN.** Bước kế gợi ý (EN-only): 🚀 deploy GRADE-003+C6+C7+C8 LÊN MAIN · hoặc Speaking (upload audio + chấm AI) · hoặc duyệt thêm cột exam_type rồi generate VSTEP từ bank.
> ⚠️ **make_fixtures.py** có thay đổi ngoài phạm vi (refactor PNG in-memory) KHÔNG phải của Claude (nhiều khả năng Anti chạy nền) — KHÔNG gộp vào commit GRADE-002, để nguyên working tree cho Anti/Đạt xử lý riêng.
> **🚀 ĐÃ DEPLOY LIVE (23/06):** FF-push `Dat→main` → `origin/main` = **`b5b075e`** (= origin/Dat). C4+C5+GRADE-002 nay LIVE. Verify thật: backend Render nộp bài→chấm trả `listening_score`/`reading_score` đúng cột mới (DB tạo bằng `create_all` lúc build → cột tự có, không cần migration thủ công); `/submissions/me` (C4) OK; frontend Vercel `/my-results` + `/admin/results/1` (C5) trả 200. **Kế tiếp gợi ý:** **GRADE-003** (Celery async — cần khi có tự luận) hoặc **VSTEP/HSK** (breadth) hoặc **proctoring** (`exam-room-ui`). **Hoãn:** SCALE-003 (load test k6/Locust).
> 🟢 **C4+C5: Anti tuân thủ đúng ranh giới** (chỉ file trong phạm vi, KHÔNG đụng log/models). ⚠️ Trước đó C1/C2/C3 Anti lặp lỗi sửa file log của Claude (đã khôi phục mỗi lần) — *memory `anti-recurring-slips`*: luôn `git status` + `git checkout Dat -- claude-progress.md session-handoff.md` mỗi nghiệm thu.

## Current State & Achievements

1. **Kiến trúc đã chốt**: `docs/kien_truc_he_thong_v2.html` (tổng thể) + `docs/kien_truc_phan_he_ra_de_tieng_anh.html/.md` (phân hệ Ra đề EN — trọng tâm M2). Tài liệu v1 đông lạnh tham chiếu.
2. **Harness hoạt động**: **33 spec** trong `specs/specs.json` (**28 active / 2 gap / 3 planned**) ↔ 8 file test; suite **35 passed / 2 skipped / 2 xfailed / 0 failed**; traceability 4 xanh; `check-architecture.sh` PASS; frontend `npm run build` PASS. **Baseline: `origin/Dat` = `origin/main` = `24f9261`, clean-state toàn xanh.**
3. **DỮ LIỆU INPUT THẬT đã đọc được** (`docs/du_lieu_input_links.md`): tải bằng `PYTHONUTF8=1 python -m gdown` về `D:\Dat-Antigravity\drive_input\` (ngoài repo) + parse openpyxl/python-docx; Sheet→export xlsx; bỏ MP3. Ma trận TOEIC (Sheet) XÁC NHẬN blueprint + luật ta đã code. Đáp án=xlsx lưới Câu/Đáp án; đề=Word tables+ảnh (đã map cấu trúc).
4. **Real Parser Track (PARSE-006 → 011) — 🎉 HOÀN TẤT (Nghe+Đọc · đề+đáp án+audio → bank, dữ liệu đối tác thật)**:
   - **A3 (`d2388e8`)**: `parse_answer_key(filepath)->dict[int,str]` (openpyxl, quét header, gộp 5 block).
   - **PARSE-006 (`8239a88` + `c65eb57`)**: `parse_listening_docx(filepath)->{set_id, items}` — bóc tách table-based `.docx` theo document order, split nhóm P3/P4 bằng line `---` trong cell, ghép wrap option/câu qua nhiều paragraph (state-machine), P1 ghi image index.
   - **PARSE-007 (`c8948fa` + vá `a6fdf99`)**: `import_listening_set(db, docx_path, key_path, audio_dir=None)` — trộn đáp án từ tệp Excel vào câu hỏi Word theo số câu, validation gate (all-or-nothing), set_id docx khớp KEY, lưu ImportBatch và commit ngân hàng ở trạng thái `draft` (save bọc try/except + rollback).
   - **Bug Question Hash Collision — Claude bắt lúc nghiệm thu trên dữ liệu THẬT**: fixture xanh nhưng `import_listening_set` trên cặp thật LT2601 chỉ ghi 75/100 câu (P1/P2 content giống nhau + options rỗng → trùng hash → nuốt 25 câu). Vá: đưa `set_id`+`number` vào `calculate_question_hash`, `set_id` vào `calculate_group_hash` (path cũ → None → tương thích ngược). **Claude verify lại file thật: 100/100 câu + 23/23 nhóm, 0 skip, idempotent ✓.** Bài học: pytest fixture xanh KHÔNG đủ — verify file thật là bắt buộc.
   - **PARSE-008 (`cfd5431`)**: `convert_doc_to_docx(filepath, out_dir=None)` — `.doc` legacy → `.docx` qua LibreOffice headless (passthrough/cache/missing-tool error). Verify thật: round-trip giữ 15 tables (cần LibreOffice; Đạt đã cài 26.2).
   - **PARSE-009 (`4668e44` + vá `0712a8a`)**: `parse_reading_docx(filepath)->{set_id, items}` — Reading parse-only, số câu 1-100 (P5 đơn / P6 4-blank / P7 passage đơn-đôi-ba + ảnh). Option extraction chắc (inline-A, all-inline `re.DOTALL`+split, sandwich, 4x2 table). **2 bug Claude bắt lúc nghiệm thu** (treo vô hạn ở inner loop + mất options 30/70 câu) — đã vá. Verify RT2605 thật: **100 câu / 0 thiếu options**.
   - **PARSE-010 (`e2941f3`)**: tổng quát `import_listening_set` → `import_exam_set(db, docx_path, key_path, exam_type, audio_dir=None)` (dispatch parser theo exam_type + set_id `(LT|RT)`); `import_listening_set`/`import_reading_set` là wrapper mỏng. **Verify dữ liệu THẬT (Claude)**: LT2601→100 câu/23 nhóm, RT2605→100 câu/20 nhóm, idempotent; LT+RT coexist 1 DB = **200 câu/43 nhóm/0 thiếu đáp án**. 🏗️ pipeline Parser thật ĐỦ Nghe+Đọc→bank.
   - **PARSE-011 (`ef2ced4`)**: `find_audio_file` + `import_exam_set` link MP3 gộp vào `audio_url` **cấp FILE** (dải/đơn, anchored regex, non-fatal, report `audio_ambiguous`). **KHÔNG cắt** (quyết định Đạt: ma trận không yêu cầu, đối tác không có timestamp, models đóng băng). Verify 15 tên file THẬT: 15/15 set map được, chỉ LT2603/LT2605 ambiguous (3 file đầu đè dải). 🎉 **Track Parser HOÀN TẤT** — `feature_list.docx-parser` → active.
4c. **DEMO TOEIC end-to-end — XONG (`44f638b`, SPEC-EXAM-001 active) — TOEIC chạy được CÓ GIAO DIỆN trên dữ liệu thật**:
   - **Backend Exam API**: `app/api/exams.py` + `app/services/exam_admin.py` + `app/schemas/exam.py` — `POST /api/v1/exams/generate`, `GET /api/v1/exams`, `GET /api/v1/exams/{id}` (đề theo part → standalone/groups → questions + options/audio/ảnh). `main.py` mount `/audio` (tùy chọn `AUDIO_DIR`). Test `tests/test_specs_exam.py` (TestClient, 200 câu).
   - **Seed `scripts/seed_toeic_demo.py`**: import THẬT LT2601+RT2605 → **backfill độ khó/topic khớp blueprint** (CHỐT CHẶN là TOPIC: parser gán all medium/None; generator đòi đa dạng topic — độ khó tự shortfall-fill) → approve 200 → sinh đề. DB demo SQLite gitignored.
   - **Frontend (Next.js 16, `params` async)**: `/` landing, `/admin` (stats + sinh đề + list), `/exam/[id]` (xem đề, đáp án tô xanh). API client `src/lib/api.ts`.
   - **Verify trình duyệt trên dữ liệu THẬT**: sinh đề từ UI (count 1→2), xem 200 câu thật (Part 5 câu + 4 options + đáp án). Console 0 lỗi. Audio/ảnh chưa bật (MP3 chưa tải/ảnh chưa trích — non-blocking). Hướng dẫn: `docs/demo_toeic_huong_dan.md`.
   - **Đề "đầy đủ như thật" + ẩn đáp án (commit `b61420d`)**: trích **ẢNH Part 1** từ .docx (`docx_images.py` → `/static`); **AUDIO Nghe** thật (tải file gộp "2601 - 2604.mp3" ~105MB qua gdown id `15HV-VV...` → `drive_input/audio/`, link qua `AUDIO_DIR`, 1 player/part); **ĐỀ MẶC ĐỊNH ẨN ĐÁP ÁN** (`GET /exams/{id}` không trả `reference_answer`; `?include_answers=true` + nút "Hiện đáp án (giáo viên)"). Verify: 6/6 ảnh load, audio 45:37 phát, toggle 200/200 đáp án.
   - **Cách chạy nhanh**: seed (`AUDIO_DIR=drive_input\audio DATABASE_URL=sqlite:///./demo_toeic.db python scripts/seed_toeic_demo.py`) → uvicorn cùng `DATABASE_URL`+`AUDIO_DIR` :8000 → `cd frontend && npm run dev` :3000. Chi tiết: `docs/demo_toeic_huong_dan.md`.
4b. **A5 Bank-Admin API — XONG (`696201e`, SPEC-BANK-003 active) — router REST ĐẦU TIÊN của dự án** (`app/api/` + `app/schemas/` trước đó rỗng):
   - `app/api/bank.py` prefix `/api/v1/bank`: `GET /questions` (filter part/status/topic/difficulty + phân trang, chỉ `exam_id IS NULL`), `PATCH /questions/{id}` (sửa bank item; 404 cho clone `exam_id IS NOT NULL` / không tồn tại), `POST /questions/approve` (bulk draft→approved + **propagate nhóm cha**), `GET /stats` (đếm part×status đối chiếu `TOEIC_BLUEPRINT`).
   - `app/services/bank_admin.py` logic thuần (API mỏng) + `app/schemas/bank.py` Pydantic v2. Endpoints CHƯA auth (auth-api not_started) — có TODO marker.
   - **Propagate nhóm cha là bắt buộc**: parser ghi cả Question lẫn QuestionGroup `status="draft"`; generator lọc CẢ HAI `==approved`. Test SPEC-BANK-003 non-tautological (stats trước seed → seed 2 draft → filter/approve → chứng minh lật nhóm cha). Nghiệm thu: 34/2/2 ×2, ruff/architecture/traceability xanh, no dep mới.
   - **conftest đổi `StaticPool`** (cần cho `:memory:` cross-thread dưới TestClient — verify thực nghiệm bỏ ra thì gãy). `feature_list.exam-admin-api` → in_progress (còn Exam CRUD + auth).
5. **SETUP (0a+0b+0c) XONG**: 0a (`2cd7988`); 0b (PR #15 → `1e139c2`) CI+ruff+pytest-cov; 0c (PR #16 → `0c265c9`) Alembic, chạy thật trên PostgreSQL 17. **MODELS ĐÓNG BĂNG từ 0c.**
6. **Pipeline Ra đề EN (B1+B2+B3+GEN-002) — generator gần hoàn chỉnh**:
   - **B1 (`45ccd09`)**: filter `approved` (BANK-001), `InsufficientBankError` + pre-check (GEN-006), `seed` qua `local_random`+`.order_by(id)` (GEN-005), `source_question_id`.
   - **B2 (`977d4d0`)**: Part 7 subset-sum đúng 54 → đề đủ 200 (GEN-001).
   - **B3 (`3c7be1f`)**: đa dạng topic, cap thích ứng `max(0.20, nhóm_lớn_nhất/tổng)` (GEN-003).
   - **GEN-002 (`ca2ce48`)**: cân bằng đáp án A/B/C/D 20-28% qua hoán vị clone.
   - → Đề sinh ra: đúng 200 câu · ma trận độ khó per-part · đa dạng topic · cân bằng đáp án · tái lập theo seed · chỉ câu approved · bank bất biến.
7. **Hạ tầng máy Đạt**: PostgreSQL 17 tại `D:\Postgres\17` (port 5432, user postgres). `psql`/`createdb` tại `D:\Postgres\17\bin\` (chưa vào PATH).
8. **Đồng bộ git**: `origin/Dat` = `origin/main` = **`24f9261`** (đã merge Dat→main + đẩy cả 2). Các nhánh feature đã xoá. Working tree sạch. Phiên demo+deploy Claude code trực tiếp trên `Dat` (Đạt chốt, vì deadline). Audio + `backend/static/` + `demo_toeic.db` gitignored.
9. **DEPLOY CLOUD LIVE**: Frontend Vercel (Root=`frontend`, env `NEXT_PUBLIC_API_BASE`=URL Render) + Backend Render (`render.yaml`, `cloud_bootstrap.py` tải data Drive theo file-id lúc BUILD rồi seed → repo sạch data đối tác). Verify qua mạng OK. URL/vận hành: memory `deploy-cloud-urls`.

## Current Gaps / In Progress

- **Generator — chỉ còn 2 hạng mục**:
  - **SPEC-MATRIX-002 toàn-đề (gap/xfail)**: tỷ lệ độ khó 25/50/25 toàn đề. ⚠️ **Giờ VƯỚNG**: sau B3, P7 có nghiệm subset-sum DUY NHẤT → độ khó P7 cố định (4E/30M/20H) → toàn đề lệch (Easy=39<45, Hard=56>55). Cần **rebalance conftest P7 difficulty** (giữ topic+size đã chốt ở B2/B3) hoặc nới nghiệm P7. Task này sửa conftest rất cẩn thận.
  - **SPEC-GEN-004 (planned, B6)**: độ trùng giữa các đề trong lô ≤40% — `source_question_id` đã sẵn từ B1; cần API/đường dẫn generate LÔ nhiều đề rồi so source giữa các cặp.
- ✅ **Parser track HOÀN TẤT (PARSE-006→011)**: parse Nghe/Đọc + convert `.doc` (LibreOffice) + merge+import chung (`import_exam_set`) + link audio cấp file. Verify dữ liệu đối tác thật (LT2601 + RT2605 = 200 câu + audio vào bank, idempotent, coexist). Audio để CẢ FILE (không cắt — quyết định Đạt); tuỳ chọn tương lai: segment theo đoạn nếu tầng thi cần.
- ⚠️ **FIXTURE conftest RẤT NHẠY (bảo trì)** — KHÔNG "dọn" thành đơn giản:
  - **Part 7**: topic+size có **nghiệm subset-sum DUY NHẤT** (loại G2+G3). Sửa phải verify lại (chứng minh trong commit `3c7be1f`).
  - **Part 4**: 3 nhóm medium "Talk" (chủ ý để GEN-003 test không tautology).
- **CI chưa test migration** (SQLite). **Celery chưa nối API** (GRADE-003) + GRADE-002 — milestone Chấm, NGOÀI M2.
- **Chờ bên ngoài**: chỉ còn **sếp xác nhận quy đổi độ khó câu→nhóm**. *(Quy ước MP3 đã chốt nội bộ.)*

## Next Session Objectives

1. **Lộ trình tiếp theo** (✅ Demo TOEIC end-to-end + ✅ deploy LIVE đã xong; chờ phản hồi sếp sau buổi trình):
   - **Đồ hoạ Part 3/4/7** (câu "look at the graphic"): `docx_images` trích được nhưng chưa map per-câu (Question không có cột số câu) — cần cách map (group order / bảng phụ).
   - **auth-api**: bịt endpoint bank + exams (đang mở; `?include_answers` cần gate giáo viên) — cần khi đưa ra ngoài thật.
   - **A6 UI duyệt bank**: approve draft→approved trên web (hiện duyệt qua seed).
   - **Nhiều bộ đề**: tải thêm LT/RT khác (sửa file-id trong `cloud_bootstrap.py`) để demo sinh đề có "lựa chọn" thật.
   - 💡 Đối chiếu `TOEIC_BLUEPRINT` ↔ Ma trận TOEIC Sheet thật → cập nhật data.
   - **Để sau (giữ nguyên theo Đạt)**: B1/VSTEP·HSK; phân hệ Chấm (GRADE-002/003, Celery); generator gaps (MATRIX-002 vướng P7, GEN-004); SCALE.
   - **Ổn định deploy**: Render free ngủ 15' → cân nhắc nâng plan / cron ping `/health` nếu cần luôn-sẵn-sàng.
2. Quy trình giữ nguyên: plan → Claude review → `DUYỆT <mã>` → code nhánh riêng → **Claude nghiệm thu XONG mới push** → merge. KHÔNG đụng `backend/app/models/`, phân hệ Chấm, `claude-progress.md`/`session-handoff.md`. ⚠️ Phiên PARSE-007 Anti lại (a) push trước khi Claude nghiệm thu, (b) tự sửa 2 file nhật ký của Claude — nhắc giữ đúng ranh giới lần sau.
3. Hạ tầng (khi rảnh): postgres service vào `ci.yml`; "Require status checks" cho `main`; hardening PARSE-002.
4. Chờ sếp xác nhận quy đổi độ khó câu→nhóm.
